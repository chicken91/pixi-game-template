import Point = PIXI.Point;

export class SizeData {
    public readonly gameWidth: number = 1980;
    public readonly gameHeight: number = 1050;

    public screenSize: Point = new Point(0, 0);
    public screenFactor: Point = new Point(0, 0);


}